public class match {
    public boolean match(String str, String pattern)
    {
        if(pattern == null)
        {
            return  false;
        }

        char curPattern = pattern.charAt(0);
        //char curStr = str

        return  false;
    }
}
